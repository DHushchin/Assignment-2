#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <filesystem>

using namespace std;

int main() {
    string dir;
    vector <string> files_arr;
    cout << "Enter the directory: "; cin >> dir;
    // ! TODO: get name's list of all files (I have some problems with "filesystem" lib)


    return 0;
}
